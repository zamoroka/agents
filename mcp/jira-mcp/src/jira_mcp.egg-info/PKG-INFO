Metadata-Version: 2.4
Name: jira-mcp
Version: 0.1.0
Summary: MCP server that provides Jira tools for pull request workflows
Requires-Python: >=3.10
Description-Content-Type: text/markdown
Requires-Dist: mcp[cli]>=1.2.0
Requires-Dist: httpx>=0.27.2

# Jira MCP (Python)

MCP server that provides Jira tools for pull request workflows.

## Tech choices

- Python MCP SDK (`mcp[cli]`) with `FastMCP`
- STDIO transport for desktop MCP client compatibility
- `httpx` for Jira REST API calls
- MCP Prompt for issue summary generation
- Modular tool registration for easy extension

## Project structure

```text
jira-mcp/
  src/jira_mcp/
    services/
      jira_api_client.py       # Jira REST API client with auth fallback
      process_my_timelogs.py   # Timelog aggregation and markdown summary
      process_issue_summary.py # Issue + MCP prompt message orchestration
      summarize_jira_issue.py  # Jira summary prompt payload generation
    tools/
      base.py                  # ToolRegistrar abstraction
      jira.py                  # MCP tool implementations
    config.py                  # .env handling and runtime settings
    server.py                  # MCP entrypoint
```

## Prerequisites

- Python 3.10+
- `uv` recommended (or use pip/venv)
- Jira API access (`JIRA_URL`, `JIRA_TOKEN`, optional `JIRA_EMAIL`)

## Setup

From `mcp/jira-mcp`:

```bash
uv venv
source .venv/bin/activate
uv pip install -e .
```

## Run MCP server

```bash
uv run jira-mcp
```

If the script is installed in your active virtual environment, you can also run:

```bash
jira-mcp
```

## Run MCP dev (inspector UI)

To launch the [MCP Inspector](https://github.com/modelcontextprotocol/inspector) for interactive testing:

```bash
uv run mcp dev src/jira_mcp/server.py
```

This starts the server and opens MCP Inspector in your browser so you can call tools manually.

## Tools

- `fetch_jira_issue_details`: fetches raw Jira issue details by key.
- `fetch_jira_my_timelogs`: fetches current user timelogs for a rolling period and returns grouped report + markdown summary.
- `add_jira_timelog`: adds a Jira worklog entry to an issue.
- `jira_issue_summary_prompt` (prompt): fetches issue details and returns prompt messages for a Jira summary.

All tools and prompts accept optional config overrides (`jiraBaseUrl`, `jiraApiToken`, `jiraEmail`, `jiraAuthType`).

## Environment file

Server reads config from `mcp/jira-mcp/.env`.
If `.env` does not exist, the server creates a placeholder automatically.

Supported variables:

```bash
JIRA_URL=
JIRA_TOKEN=
JIRA_EMAIL=
JIRA_AUTH_TYPE=auto
```

Precedence:
1. Tool call arguments
2. Process environment variables
3. `.env` values

## Auth behavior

- `jiraAuthType=auto`: tries Bearer first; if Jira returns `401` and `jiraEmail` exists, retries with Basic auth.
- `jiraAuthType=bearer`: only Bearer auth.
- `jiraAuthType=basic`: only Basic auth (`jiraEmail` required).

## MCP client config example

Example `~/.mcp.json` entry:

```json
{
  "servers": {
    "jira-mcp": {
      "type": "stdio",
      "command": "uv",
      "args": [
        "--directory",
        "~/.agents/mcp/jira-mcp",
        "run",
        "jira-mcp"
      ],
      "env": {
        "JIRA_URL": "https://jira.example.com",
        "JIRA_TOKEN": "your_jira_token",
        "JIRA_EMAIL": "user@example.com",
        "JIRA_AUTH_TYPE": "auto"
      }
    }
  }
}
```

If you already use `mcp/jira-mcp/.env`, you can omit `env` values that are already present there.
